# Zeabur 部署配置
services:
  - name: btc-15min-trader
    type: worker
    buildCommand: pip install -r requirements.txt --default-timeout=300
    startCommand: bash start.sh
    env:
      - PYTHON_VERSION=3.9
