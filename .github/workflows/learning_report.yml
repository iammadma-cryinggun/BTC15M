name: 学习报告自动生成

on:
  schedule:
    # 每12小时运行一次（UTC时间 2:00 和 14:00）
    - cron: '0 2,14 * * *'
  workflow_dispatch:  # 手动触发

jobs:
  generate_report:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout代码
      uses: actions/checkout@v3

    - name: 设置Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 安装依赖
      run: |
        pip install -r requirements.txt

    - name: 生成学习报告
      run: |
        python generate_learning_report.py

    - name: 上传报告
      uses: actions/upload-artifact@v3
      with:
        name: learning-report
        path: learning_report.html

    - name: 提交报告到仓库
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add learning_report.html || true
        git commit -m "📊 学习报告自动生成 [skip ci]" || echo "No changes to commit"
        git push
