name: 学习报告自动生成

on:
  schedule:
    # 每12小时运行一次（UTC时间 2:00 和 14:00）
    - cron: '0 2,14 * * *'
  workflow_dispatch:  # 手动触发

permissions:
  contents: write  # 允许提交报告到仓库

jobs:
  generate_report:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout代码
      uses: actions/checkout@v4

    - name: 设置Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: 安装依赖
      run: |
        pip install -r requirements.txt

    - name: 生成学习报告
      run: |
        python generate_learning_report.py

    - name: 检查报告文件
      run: |
        if [ -f learning_report.html ]; then
          echo "✅ 报告已生成: learning_report.html"
          ls -lh learning_report.html
        else
          echo "⚠️  报告未生成（可能数据库不存在或无数据）"
          exit 0  # 继续执行，不阻断流程
        fi

    - name: 上传报告
      if: hashFiles('learning_report.html') != ''
      uses: actions/upload-artifact@v4
      with:
        name: learning-report
        path: learning_report.html
        retention-days: 30

    - name: 提交报告到仓库
      if: hashFiles('learning_report.html') != ''
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add learning_report.html || true
        git commit -m "📊 学习报告自动生成 [skip ci]" || echo "No changes to commit"
        git push || echo "Push failed (可能没有新变更)"

