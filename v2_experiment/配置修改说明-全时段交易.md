# 配置修改说明 - 2026-03-01 23:21

## 📝 修改内容

### 1. ✅ 去掉 6 分钟限制

**修改前**：
```python
if minutes_to_expiry > 6:
    if is_nuke:
        # 核弹级信号才能穿透
        pass
    else:
        print(f"拦截: 剩余{minutes_to_expiry}分钟(>6分钟)，处于无序震荡期")
        return 0.0  # 拒绝入场
```

**修改后**：
```python
# 🔴 已移除 6 分钟限制，允许全时段入场
if minutes_to_expiry > 10:  # 10-15 分钟
    multiplier *= 0.9  # 仓位 90%
elif minutes_to_expiry > 5:  # 5-10 分钟
    multiplier *= 1.0  # 仓位 100%（黄金窗口）
elif minutes_to_expiry > 2:  # 2-5 分钟
    multiplier *= 1.0  # 仓位 100%
else:  # < 2 分钟
    multiplier *= 0.5  # 仓位 50%（快到期）
```

**影响**：
- 现在可以在 15 分钟周期的任何时间入场
- 10-15 分钟：90% 仓位
- 2-10 分钟：100% 仓位（最佳窗口）
- < 2 分钟：50% 仓位（风险保护）

---

### 2. ✅ 恢复止盈止损功能

**修改前**：
```python
'enable_stop_loss': False,       # 🔴 禁用止盈止损
'enable_trailing_tp': False,     # 🔴 禁用追踪止盈
'enable_absolute_tp': False,     # 🔴 禁用绝对止盈
```

**修改后**：
```python
'enable_stop_loss': True,        # ✅ 启用止盈止损
'enable_trailing_tp': True,      # ✅ 启用追踪止盈
'enable_absolute_tp': True,      # ✅ 启用绝对止盈
```

**影响**：
- 所有止盈止损机制已恢复
- 追踪止盈：价格涨到 0.75 后，从最高点回撤 5¢ 触发
- 绝对止盈：价格达到 0.92 时强制平仓
- 常规止损：价格跌破止损线触发

---

### 3. ✅ 确认止损是 50%

**修改前**：
```python
'max_stop_loss_pct': 0.80,  # 80% 止损
```

**修改后**：
```python
'max_stop_loss_pct': 0.50,  # 🔴 50% 止损（确认）
```

**影响**：
- 止损触发条件：价格下跌 50% 时触发
- 例如：入场价 0.40 → 止损价 0.20
- 更严格的风险控制

---

### 4. ✅ 调整止盈参数

**修改**：
```python
'take_profit_pct': 0.30,  # 30% 止盈（从 90% 调整）
```

**影响**：
- 止盈触发条件：价格上涨 30% 时触发
- 例如：入场价 0.40 → 止盈价 0.52
- 更合理的止盈目标

---

## 📊 新的交易策略

### 入场时机
- **全时段入场**：不再限制 6 分钟
- **最佳窗口**：2-10 分钟（100% 仓位）
- **早期窗口**：10-15 分钟（90% 仓位）
- **晚期窗口**：< 2 分钟（50% 仓位）

### 止盈止损
- **止盈**：30% 收益或追踪止盈触发
- **止损**：50% 亏损时触发
- **追踪止盈**：价格涨到 0.75 后，从最高点回撤 5¢
- **绝对止盈**：价格达到 0.92 时强制平仓

### 风险控制
- **止损更严格**：从 80% 改为 50%
- **止盈更合理**：从 90% 改为 30%
- **追踪止盈**：锁定高位利润
- **绝对止盈**：防止最后时刻反转

---

## 🎯 适用场景

这套配置适合：
- ✅ 全时段交易（不限制入场时间）
- ✅ 主动风险管理（止盈止损保护）
- ✅ 追求稳定收益（30% 止盈目标）
- ✅ 严格止损（50% 止损线）

---

## ⚠️ 注意事项

1. **止损更严格**：50% 止损意味着更容易触发，但也更好地保护本金
2. **止盈更频繁**：30% 止盈会更频繁触发，可能错过更大收益
3. **全时段入场**：早期入场（10-15 分钟）信号可能不稳定
4. **追踪止盈**：需要价格先涨到 0.75，适合强势行情

---

## 🔧 如果需要调整

### 调整止损（更宽松）
```python
'max_stop_loss_pct': 0.60,  # 60% 止损（更宽容）
```

### 调整止盈（更激进）
```python
'take_profit_pct': 0.50,  # 50% 止盈（更高目标）
```

### 禁用追踪止盈（简化策略）
```python
'enable_trailing_tp': False,  # 禁用追踪止盈
```

---

**修改日期**：2026-03-01 23:21  
**修改人**：Claude (OpenClaw)
