# 止盈开关配置说明

## 📝 修改内容

### 1. 新增配置项（在 CONFIG['risk'] 中）

```python
'risk': {
    # ... 其他配置 ...
    
    # [止盈开关] 可以单独控制每种止盈机制
    'enable_trailing_tp': False,    # 🔴 禁用追踪止盈（0.75激活，回撤5¢触发）
    'enable_absolute_tp': False,    # 🔴 禁用绝对止盈（0.92强制平仓）
}
```

### 2. 止盈机制说明

#### 追踪止盈（Trailing Take-Profit）
- **触发条件**：价格涨到 0.75 后，从最高点回撤 5¢（0.05）
- **示例**：入场 0.40 → 涨到 0.85（最高）→ 跌到 0.80 → 触发平仓
- **适用场景**：入场时间早（剩余 > 8 分钟），有足够时间让价格上涨并回撤
- **当前状态**：🔴 **已禁用**

#### 绝对止盈（Absolute Take-Profit）
- **触发条件**：价格达到 0.92 时强制平仓
- **目的**：防止最后 1 秒"画门"（价格突然反转）
- **适用场景**：价格已经很高，不想赌最后结算
- **当前状态**：🔴 **已禁用**

## 🎯 为什么禁用？

根据你的交易策略：
- **入场时间**：最后 6 分钟才判断入场
- **策略目标**：赌 15 分钟到期结算，持有到底
- **时间不足**：6 分钟内价格很难涨到 0.75 再回撤 5¢
- **收益最大化**：如果方向正确，到期价格是 1.00（100% 收益）

## 🔧 如何启用？

如果你想重新启用某个止盈机制，只需修改配置：

```python
'risk': {
    'enable_trailing_tp': True,     # ✅ 启用追踪止盈
    'enable_absolute_tp': True,     # ✅ 启用绝对止盈
}
```

## 📊 建议的使用场景

### 场景 1：早期入场（剩余 > 8 分钟）
```python
'enable_trailing_tp': True,   # 启用追踪止盈
'enable_absolute_tp': True,   # 启用绝对止盈
```

### 场景 2：中期入场（剩余 4-8 分钟）
```python
'enable_trailing_tp': False,  # 禁用追踪止盈（时间不够）
'enable_absolute_tp': True,   # 启用绝对止盈（保护高位）
```

### 场景 3：晚期入场（剩余 < 4 分钟）- 当前配置 ⭐
```python
'enable_trailing_tp': False,  # 禁用追踪止盈
'enable_absolute_tp': False,  # 禁用绝对止盈
# 持有到期，赌结算结果
```

## ⚠️ 注意事项

1. **功能未删除**：代码逻辑完整保留，只是通过开关控制
2. **随时可启用**：修改配置后重启程序即可生效
3. **独立控制**：两个止盈机制可以单独开关
4. **默认禁用**：当前配置为禁用状态，适合你的 6 分钟入场策略

## 🚀 测试建议

1. **先禁用运行**：观察持有到期的收益情况
2. **记录数据**：对比启用/禁用止盈的胜率和收益
3. **动态调整**：根据实盘数据决定是否启用

---

**修改日期**：2026-03-01  
**修改人**：Claude (OpenClaw)
